<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="320dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@drawable/bg_white_rounded_16dp"
    android:gravity="center">

    <ImageButton
        android:id="@+id/btnClose"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="end"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_close" />

    <ImageView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/img_mascot_shopping" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Vui lòng đăng nhập tài khoản TechMate để xem ưu đãi và thanh toán dễ dàng hơn."
        android:textAlignment="center"
        android:textColor="#333333"
        android:textSize="16sp"
        android:layout_marginTop="15dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="25dp"
        android:weightSum="2">

        <Button
            android:id="@+id/btnRegisterNav"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Đăng ký"
            android:textColor="#D70018"
            android:backgroundTint="@color/white"
            android:strokeColor="#D70018"
            android:strokeWidth="1"
            android:layout_marginEnd="5dp"/>

        <Button
            android:id="@+id/btnLoginNav"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Đăng nhập"
            android:backgroundTint="#D70018"
            android:textColor="@color/white"
            android:layout_marginStart="5dp"/>
    </LinearLayout>
</LinearLayout>